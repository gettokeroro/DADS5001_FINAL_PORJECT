# Core — pin upper bound to prevent breaking changes from pandas 3.x / numpy 2.x
streamlit>=1.32,<2.0
duckdb>=0.10,<2.0
pandas>=2.0,<3.0
numpy>=1.24,<3.0                # numpy 2.x for Py 3.13+ · cap < 3.0 for safety
scikit-learn>=1.3,<2.0
plotly>=5.18,<7.0
matplotlib>=3.7,<4.0
seaborn>=0.13,<1.0

# AI / LLM (Phase 4)
google-generativeai>=0.7,<1.0   # Gemini
pydantic>=2.5,<3.0              # structured output validation
# anthropic>=0.18,<1.0          # Claude (uncomment if switching)
# openai>=1.12,<2.0             # OpenAI (uncomment if switching)

# External DB (Phase 5 — uncomment when integrated)
# supabase>=2.3
# psycopg2-binary>=2.9

# Cloud storage (Phase 9 — ข้อ 3 spec)
snowflake-connector-python>=3.7,<4.0       # Snowflake: TRAIN_MATRIX + DISEASE_DRUG + HOSPITALS
tomli>=2.0,<3.0 ; python_version < "3.11"  # TOML parser for migrate_to_snowflake.py (stdlib in 3.11+)
pymongo>=4.6,<5.0                          # MongoDB Atlas (Phase 9b — symptom_dict + disease_prev + ai_sessions)

# Dev (optional — comment out on Streamlit Cloud to speed up cold start)
# jupyter>=1.0,<2.0
# nbformat>=5.9,<6.0
