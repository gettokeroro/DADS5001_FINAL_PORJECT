# Core — pin upper bound to prevent breaking changes from pandas 3.x / numpy 2.x
streamlit>=1.32,<2.0
duckdb>=0.10,<2.0
pandas>=2.0,<3.0
numpy>=1.24,<2.0
scikit-learn>=1.3,<2.0
plotly>=5.18,<7.0
matplotlib>=3.7,<4.0
seaborn>=0.13,<1.0

# AI / LLM (Phase 4 — uncomment when ready)
# google-generativeai>=0.5     # Gemini
# anthropic>=0.18              # Claude
# openai>=1.12                 # OpenAI
# pydantic>=2.5                # output validation

# External DB (Phase 5 — uncomment when integrated)
# supabase>=2.3
# psycopg2-binary>=2.9

# Dev (optional — comment out on Streamlit Cloud to speed up cold start)
# jupyter>=1.0,<2.0
# nbformat>=5.9,<6.0
